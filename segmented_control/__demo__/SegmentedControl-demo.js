/**
 * TEAM: frontend_infra
 *
 * @flow
 */

import {type DemoFile} from "../../demoTypes";
import SegmentedControlBasicUsage from "./SegmentedControlBasicUsage.demo";

const demos: DemoFile = {
  demos: [
    {
      type: "live",
      example: SegmentedControlBasicUsage,
    },
  ],
};

export default demos;
