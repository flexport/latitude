/**
 * TEAM: frontend_infra
 *
 * @flow strict
 */

const latitudeSectionTitle = "Latitude";

const sections = Object.freeze({
  dataDisplay: `${latitudeSectionTitle}|Data Display`,
  dataEntry: `${latitudeSectionTitle}|Data Entry`,
  buttons: `${latitudeSectionTitle}|Buttons`,
  errorBoundary: `${latitudeSectionTitle}|Error Boundary`,
  filter: `${latitudeSectionTitle}|Filter`,
  general: `${latitudeSectionTitle}|General`,
  interaction: `${latitudeSectionTitle}|Interaction`,
  navigation: `${latitudeSectionTitle}|Navigation`,
  table: `${latitudeSectionTitle}|Table`,
  takeover: `${latitudeSectionTitle}|Takeover`,
  toast: `${latitudeSectionTitle}|Toast`,
  formLayout: `${latitudeSectionTitle}|Form Layout`,
  modals: `${latitudeSectionTitle}|Modals`,
  formulaOne: `${latitudeSectionTitle}|Formula One`,
  popover: `${latitudeSectionTitle}|Popover`,
  layout: `${latitudeSectionTitle}|Layout`,
});

export default sections;
