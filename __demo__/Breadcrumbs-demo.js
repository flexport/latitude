/**
 * TEAM: frontend_infra
 * WATCHERS: notandrewkaye
 * @flow
 */

import {type DemoFile} from "../demoTypes";
import BreadcrumbsBasicUsage from "./BreadcrumbsBasicUsage.demo";

const demos: DemoFile = {
  demos: [
    {
      type: "live",
      example: BreadcrumbsBasicUsage,
    },
  ],
};

export default demos;
