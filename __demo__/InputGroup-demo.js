/**
 * TEAM: frontend_infra
 * WATCHERS: notandrewkaye
 * @flow
 */

import {type DemoFile} from "../demoTypes";
import InputGroupSmushInputs from "./InputGroupSmushInputs.demo";

const demos: DemoFile = {
  demos: [
    {
      type: "live",
      example: InputGroupSmushInputs,
    },
  ],
};

export default demos;
