/**
 * TEAM: frontend_infra
 * @flow
 */

import DrawerBasicUsage from "./DrawerBasicUsage.demo";
import {type DemoFile} from "../demoTypes";

const demos: DemoFile = {
  demos: [
    {
      type: "live",
      example: DrawerBasicUsage,
    },
  ],
};

export default demos;
