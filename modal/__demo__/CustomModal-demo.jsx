/**
 * TEAM: frontend_infra
 * @flow
 */

import {type DemoFile} from "../../demoTypes";
import CustomModalBasic from "./CustomModalBasic.demo";

const demos: DemoFile = {
  demos: [
    {
      type: "live",
      example: CustomModalBasic,
    },
  ],
};

export default demos;
